// ------------------------------------
// Generated by Glue on 2025-11-03
// Source: config_schema.glue
// ------------------------------------

{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "GlueConfigSchema",
    "type": "object",
    "properties": {
        "generation": {
            "$ref": "#/$defs/GlueConfigSchema::Generation",
            "description": "Configuration for code generation (`glue gen [...]`)"
        }
    },
    "$defs": {
        "GlueConfigSchema": {
            "type": "object",
            "properties": {
                "generation": {
                    "$ref": "#/$defs/GlueConfigSchema::Generation",
                    "description": "Configuration for code generation (`glue gen [...]`)"
                }
            }
        },
        "GlueConfigSchema::Generation": {
            "type": "object",
            "properties": {
                "python_pydantic": {
                    "$ref": "#/$defs/GlueConfigSchema::Generation::PythonPydantic",
                    "description": "Configurations for Python code generation using Pydantic (`glue gen py-pydantic [...]`)"
                },
                "watermark": {
                    "$ref": "#/$defs/GlueConfigSchema::Generation::Watermark",
                    "description": "Mode for the watermark at the top of the generated files. Defaults to \"short\" if not specified."
                }
            }
        },
        "GlueConfigSchema::Generation::PythonPydantic": {
            "type": "object",
            "properties": {
                "base_model": {
                    "type": "string",
                    "description": "The full import path for the base model class to inherit from. Defaults to `pydantic.BaseModel` if not specified."
                }
            }
        },
        "GlueConfigSchema::Generation::Watermark": {
            "type": "string",
            "description": "Mode for generating the watermark comment at the top of generated files",
            "enum": [
                "full",
                "short",
                "none"
            ]
        }
    }
}